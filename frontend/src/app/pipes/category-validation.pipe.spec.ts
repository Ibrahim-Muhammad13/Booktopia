import { CategoryValidationPipe } from './category-validation.pipe';

describe('CategoryValidationPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryValidationPipe();
    expect(pipe).toBeTruthy();
  });
});
